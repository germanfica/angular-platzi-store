import { RemoveDoubleProductsPipe } from './remove-double-products.pipe';

describe('RemoveDoubleProductsPipe', () => {
  it('create an instance', () => {
    const pipe = new RemoveDoubleProductsPipe();
    expect(pipe).toBeTruthy();
  });
});
